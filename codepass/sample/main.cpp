#include <iostream>

#define DEBUG true

using namespace std;
using pii = pair<int, int>;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    if (DEBUG)
    {
        freopen("input.txt", "r", stdin);
        // freopen("output.txt", "w", stdout);
    }

    int N;

    cin >> N;

    for (int i = 1; i <= N; ++i)
    {
    }

    return 0;
}