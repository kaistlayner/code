#include <iostream>

#define DEBUG true

using namespace std;

int main() {
  if (DEBUG) {
    freopen("output.txt", "w", stdout);
    freopen("input.txt", "r", stdin);
  }
  int N;

  cin >> N;

  for (int i = 1; i <= N; ++i) {
  }

  return 0;
}