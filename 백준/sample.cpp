#include <iostream>

#define DEBUG false

using namespace std;

int main() {
  if (DEBUG) {
    freopen("output.txt", "w", stdout);
    freopen("input.txt", "r", stdin);
  }
  int N;

  cin >> N;

  for (int i = 1; i <= N; ++i) {
  }

  return 0;
}